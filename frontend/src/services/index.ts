/**
 * Barrel export cho tất cả API services.
 *
 * Cho phép import gọn gàng:
 * import { examApiService, authApiService } from '../services';
 */
export { examApiService, authApiService } from './apiService';
